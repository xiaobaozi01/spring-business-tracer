---
description: 查看Spring全项目分析run的阶段、覆盖率、验证率、租约和待处理入口
agent: spring-business-orchestrator
subtask: false
---

加载`spring-business-tracer`和`references/state-machine.md`。读取run `$ARGUMENTS`，只报告模式/baseline、阶段、批次、租约、PUBLISHED/REUSED/缺口、全部V2指纹、graphHash/topologyRootHash和下一步；旧run显示LEGACY_READ_ONLY/FULL_REBASE_REQUIRED。
