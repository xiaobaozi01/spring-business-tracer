<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acme.order.mapper.OrderMapper">
    <select id="existsByBizNo" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM t_order
        WHERE biz_no = #{bizNo}
    </select>

    <insert id="insertOrder">
        INSERT INTO t_order (biz_no, customer_id, status)
        VALUES (#{bizNo}, #{customerId}, 'CREATED')
    </insert>

    <select id="findIdByBizNo" resultType="long">
        SELECT id
        FROM t_order
        WHERE biz_no = #{bizNo}
    </select>
</mapper>
